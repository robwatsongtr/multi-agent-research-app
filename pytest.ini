[pytest]
markers =
    slow: marks tests as slow (requiring real API calls, may cost tokens)

# Default: skip slow tests
# Run slow tests with: pytest -m slow
# Run all tests with: pytest -m ""
addopts = -m "not slow"
